@model IEnumerable<IronDome.Models.Volley>

@{
    ViewData["Title"] = "Volleys";
}

<h2 class="text-primary">Volleys Management</h2>

<table class="table table-hover mt-3">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LaunchDate)
            </th>
            <th>Launchers</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-primary">
                    @Html.DisplayFor(modelItem => item.LaunchDate)
                </td>
                <td class="text-primary">
                    @string.Join(", ", item.Launchers.Select(l => l.Name))
                </td>
                <td>
                    <div class="btn-group" role="group" aria-label="Actions">
                        <a asp-action="Edit" asp-route-id="@item.Id" asp-route-attackerId="@ViewBag.AttackerId" class="btn btn-sm btn-primary">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                        <a asp-action="Details" asp-route-id="@item.Id" asp-route-attackerId="@ViewBag.AttackerId" class="btn btn-sm btn-info">
                            <i class="bi bi-info-circle"></i> Details
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" asp-route-attackerId="@ViewBag.AttackerId" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash"></i> Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" asp-route-attackerId="@ViewBag.AttackerId">
    <button class="btn btn-outline-success">
        Create New
    </button>
</a>
